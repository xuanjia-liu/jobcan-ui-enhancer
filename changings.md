# Jobcan UI Enhancer - プラグイン概要＆機能説明

Jobcan UI Enhancerは、Jobcanのユーザー体験を大幅に向上させるために作られたブラウザ拡張機能です。このプラグインは、ユーザーインターフェースの改善、時間管理機能の強化、情報の視認性向上など、サービスの利用をさらに快適かつ効率的にするための多彩な機能を提供します。以下に、各機能の詳細とその役割について説明します。

## 未リリース

### トップページ（打刻画面）

- **打刻カードの枠を外しました。** 時計と進捗パネルがすでに独自の面を持っているため、二重の箱になっていた背景・枠線・ヘッダーをなくしました。
- **「勤務中」をカードヘッダーから時計の中に移しました。** 状態はピル表示になり、Jobcan側の更新にそのまま追従します。
- **打刻場所・備考・通常／夜勤モードを「打刻詳細設定」ボタンに畳みました。** ボタンはPUSHと同じ行に並びます。選択中の打刻場所はボタン上に表示されるので、開かなくても確認できます（入力欄はJobcan純正のまま移動しているだけです）。
- **時計の数字に「札束」の厚みをつけました。** 各タイルの下に、後ろに重なっている札の小口が2枚ぶん、合わせて5px程度のぞきます。小口ごとに、上端に細い光の線が1本だけ入ります（陰影だけでは2枚が1本の暗い帯に潰れて単なる面取りに見えますが、線は1本で足りて、しかも「光の当たる側」でなければなりません。下端にも継ぎ目の線を入れて3枚重ねた版は、14px程の中に細線が6本並んでモアレのようになりました）。あくまで「後ろに何かある」ことが分かる程度に留めています——主役は数字なので、それ自体が目に付く厚みは数字と競合します。厚みは下方向のみで、奥の札ほど左右が細くなります（横にずらすと、同じグループ内の数字と数字のあいだ2〜5pxの隙間が暗く埋まってしまうためです）。厚みのぶんはHH/MM/SSのパネルの下余白で確保しているので、束がパネルからはみ出すことはありません。要素は追加しておらず、疑似要素2つだけで描いています。
- **時計の上にサマリー行を追加しました。** 本日勤務時間・進捗率・残り時間・目標達成時刻の4枚。数値は進捗バーと同じ打刻データから算出しています。幅が足りないときは折り返さず、アイコン→補助テキストの順に省きます。
- **進捗バー下の文言を削除しました。** 同じ内容がサマリー行にあるためです。打刻エラーと「◯分前の記録」の注意書きは本日勤務時間タイルに移しました。
- **進捗バーの外枠（パネル）をなくしました。** バーだけが残ります。
- **バーの各区間にマウスを乗せると内訳が出ます。** 勤務・休憩・昼休憩（推定）・勤務外／未経過ごとに、時刻と長さを表示します。
- **「以下の項目の確認をお願いいたします。」カードを廃止しました。** 打刻漏れ・打刻エラーなどの件数は、0件でないものだけサマリー行にバッジで出ます（リンク先はJobcan標準のまま）。
- **「管理者からのお知らせ」カードを廃止しました。** お知らせがあるときだけ、打刻エリアの下に1件ずつカードで表示します。ないときは何も出ません。
- **時計カードに背景アニメーションを6種類追加しました。** ポップアップの「表示設定 → 背景アニメーション」から選べます（「なし」を含む7択、初期値は「パルス」）。
  - **パルス（同心円）** … 中央から同心円がゆっくり広がります。
  - **ウェーブメッシュ** … 遠近のついた1枚のワイヤーフレーム面です。横線（行）と、カードの上方にある消失点へ収束する縦線（列）でメッシュを描き、そこを周期の異なる3つのうねりが流れます。行と行のあいだをごく薄く塗ることで、手前の帯が奥の線を隠し、格子ではなく「面」に見えるようにしています。カーソルは「明かり」です（絵そのものは動きません）。カーソルのまわりの一定範囲だけ、線が不透明側・濃い側へ引き上げられるので、ふだんはフェードで消えている部分——奥の行や先細りした左右の端——がカーソルの下で浮かび上がり、通り過ぎるとまた沈みます。位置も強さもイージングしてあるので、カードに入った瞬間・出た瞬間に点滅しません。ただしメッシュの「端」だけは光りません。左右の端の先細りと奥のフェードは遠近感のためだけでなく、面が有限の長方形であることを隠すためのものなので、明かりがそこまで届くと最外周の列と最後の行がはっきりした線として出てしまい、無限に続くグリッドに見えなくなります。そこで明かりには、外周にいくほど0に落ちるマスクをかけています（フェードで隠れていたものは出せるが、面が終わっている場所だけは出せない、という配分です）。**被写界深度（ピントのボケ）があります。** 面の中ほどの少し奥にピント面を置き、そこから離れるほどボケます。ボケ量は画面の縦位置に対して素直に比例します（錯乱円は 1/距離 の差に比例し、この投影では 1/距離 が画面Yに比例するため、平面のボケはピント線の上下で直線になります）。ボケた線は「同じインクをより広い帯に散らしたもの」として描いています——線は太さの違う3本の重ね描き（芯・中・にじみ）で、ボケると3本すべてが同じ倍率で太くなり、そのぶん濃度が下がります。中間の1本はボケているときだけ引かれるので、ピントが合っている行は従来どおり2本のままで、ボケた行には硬い芯が残りません。実際のぼかし（`filter: blur()` や毎フレームのオフスクリーン合成）は使っておらず、値はすべてレイアウト時に確定しています。縦線は1本で全ての奥行きをまたぐため、ボケ量を4段階に量子化した区間に分けて描き、ボケの大きい区間には横線と同じように「にじみ」を足しています。**色はグラデーションになります。** 面の傾きが決めるのはパレット全体ではなく「窓」の中の振れ幅で、窓そのものが奥から手前へ（および左から右へ）スライドします。奥は濃い状態色、手前は明るい状態色になり、山の稜線はその奥行きの色のまま光ります。両端とも状態色から作っているので、勤務中と退室中でそれぞれのグラデーションになります。明暗は面の傾きから出しています（うねりの合成は解析的な式なので、その微分＝法線もそのまま求まります。周囲の点をサンプリングする必要はありません）。白いカードでは、明るい部分ほど「濃い色」を使います（白の上では色を重ねることは暗くすることにしかならず、明るい色＝ほぼ白の線は見えないためです。ダークモードでは通常どおり明るい側を使います）。旧版は48,000点のThree.jsスケッチを移植したドットの群れでしたが、このカードのサイズでは点が面につながらず、名前どおりのメッシュにもなっていなかったため作り直しました。描画も1フレームあたり約760回のスプライト転送から約55本のパス描画に減っています。
  - **パーティクル** … 半透明の小さなドットがゆらぎながら漂います。
  - **メッシュグラデーション** … カード下端の弧から30個のぼかし球が前後2列でゆっくり立ちのぼり、重なり合ってひと続きのグラデーションになります（後列は半個ぶん横にずらしてあり、前列の球と球のすき間をちょうど埋めます。1列のまま数だけ増やしても、同じ列の球は同じ動きをするのですき間が縦に通ったままになります）（旧「ブラーブロブ」。大きな塊が4つ漂うだけだったものを、パーティクルツールの書き出し `particle-export.svg` をもとに作り直しました）。ライトモードでは状態色を明るく・鮮やかにしてから重ねています（色相は状態色のままです）。白いカードの上では、色を重ねることは必ず「暗くする」ことにしかならないため、勤務中の濃い青（#0066ff）をそのまま薄く敷くと紫がかった濁った灰色になってしまいます。先に白へ寄せて淡い色にしてから彩度を戻すことで、同じ色相のまま澄んだ空色として出るようにしています（濁りの対処は「薄くする」ではなく「明るくする」ほうです。透明度を下げるだけだと、ただ灰色になります）。退室中のグレーはほぼ変化しません。**カーソルにわずかに反応します。** 前後2列が視差でカーソルと反対側へずれます（後列は前列の4割だけ動くので、2列が別の奥行きにあるように見えます）。動く量はカード幅の2.6%までで、1秒前後かけてイージングするため、「マウスを追いかけるもの」ではなく「光が動いた」ように見えます。
  - **オービット** … 傾きの違う3本の楕円軌道が回り、中央がやわらかく発光します。
  - **ゴッドレイ（光の柱）** … カード外の一点から広がる5本の光の柱。柱の中を光の波が流れ、塵（ちり）が約100粒、光の向きに沿って流れます。Figmaプラグイン「Shadow Studio」のGod Rayモードの考え方を移植しています（頂点を1つに揃える／柱は頂点から広がる三角形／塵は自分の光線に沿って進み遠くなるほど大きくなる／白→アクセント色→濃色の減衰）。柱の縁のぼかしは円錐グラデーションのマスクで、先端のぼかしは `filter: blur()` で作っています（横方向と奥方向で担当が別です）。塵だけはぼかしも発光もなしで、画面内で唯一輪郭のある要素にしています。**カーソルにわずかに反応します。** 光源（頂点）を軸に5本の柱がまとめてカーソル側へ最大2.4度傾きます。柱を個別にカーソルへ向けるのではなく頂点まわりの回転1つで済ませているので、「1点から出た光」という形は崩れません。傾きの速さ（1.8秒）は元のゆらぎ（54秒周期）に合わせてあり、ゆらぎがずれた程度に見えます。
- **背景の色は勤務状態に追従します。** 勤務中は青、退室中はグレー、状態が読めていない間は打刻前と同じニュートラルです。退室中は明度と速度をさらに落としてあります（離席中のカードが勤務中と同じ賑やかさになるのは違うため）。色は時計の数字と同じ `--color-clock-*` 変数から作っているので、数字と背景の色がずれることはありません。
- **退室中のゴッドレイは止まります。** 光の波の流れをなくし（濃淡の縞は残るので柱の奥行きはそのままです）、塵の速さを勤務中の1/3にしています。
- **OSの「視差効果を減らす」設定を尊重します。** 有効な場合は絵柄はそのまま残し、動きだけを止めます。

### 工数入力設定ページ（工数管理）

- **「デフォルト」行の列幅を工数実績編集ページに揃えました。** チェックボックス列がないぶん、編集ページ向けの列幅指定が1列ずつずれて当たっており、プロジェクト列が40pxまで潰れて見出しが1文字ずつ縦に折り返していました。実測で 648 / 150 / 200 / 96px（編集ページは 666 / 150 / 200 / 96px）になります。

### 優先表示設定ページ（工数管理）

- **ダークモードに対応しました。** これまで対象外で、「選択できる項目」「優先表示」の2つの表はJobcan側の文字色（#333）のまま暗い背景に乗っており、ほぼ読めない状態でした。表の見出し・セル・枠線・行ホバー、プロジェクト／タスクのタブ、読み込み中の白いオーバーレイをダークの配色に合わせています。
- **「追加」「取消」ボタンの2行折り返しを直しました。** 幅の詰まった列で「追」「加」と縦に割れていたものを1行に収めます。
- **「移動」列のドラッグハンドルを表示し直しました。** Bootstrap 3のWebフォント（Glyphicons）のアイコンで、拡張のフォント指定下では表示されなくなっていたため、CSSで3本線を描いています。

### 申請一覧ページ（休暇申請・残業申請・休日出勤申請）

- **「新規申請」ボタンを見出しの右隣に移しました。** これまではタイトルの下、カード内で1行を占めていたものを、`h2` と同じ行の右端に配置しています。ボタン自体はJobcan純正のまま移動しているだけです。

### サイドメニュー

- **角丸を16pxに広げ、下端の余白を詰めました。** 他のカードと同じ角丸になり、言語セレクトの下の二重の余白がなくなります。

### 内部

- **拡張の再適用が途中で止まる問題を修正しました。** DOMの変化が続くページでは、デバウンスが毎回リセットされて `applyEnhancements()` が二度と実行されないことがありました（実測：数分間停止）。最大待ち時間を設けて、必ず実行されるようにしています。あとから非同期で入るコンテンツ（「以下の項目の確認」の件数など）が反映されなかった原因です。
- **カードの角丸と影を時計に揃えました。** 角丸16px、影は他のカードと同じトークンです。
- **入れ子になった余白を整理しました。** 画面端から時計カードまでに `container-fluid` 15px＋`main.col` 8px＋`jbc-container` 20px+15px＋`col` 8px と重なっていたため、内側の2つを削除。カード内のグリッドの溝も外し、サマリーと時計の左端を揃えました。
- **打刻詳細設定とPUSHのボタン行を中央寄せに戻しました。** カード全体の余白整理でこの行まで左寄せになっていましたが、真上の時計と中心を揃えます。
- **打刻画面のレイアウトを一新しました。** サマリータイル4枚はページ背景の上に浮かぶ独立したカードになり、その下の大きな白いカードに時計・打刻詳細設定＋PUSH・タイムラインをまとめました。カードは画面の高さいっぱいに広がり、時計とボタンは中央に、タイムラインはカードの下端に配置されます。
- **タイムラインに凡例を付けました。** 勤務時間（青）・休憩時間（ティール、打刻された休憩）・休憩（推定）（黄色、打刻がない日の昼休憩）・未確定（グレー）・現在時刻（赤いバー）。色はバー本体と同じ変数を使っているため、ずれることはありません。
- **凡例をその日に実際に描かれた区間だけに絞りました。** 休憩の打刻がない日でも「休憩時間」が並んでいましたが、バーに出ている色だけを表示します（現在時刻マーカーは常に表示）。
- **現在時刻マーカーが白く見えていたのを直しました。** 幅4pxに2pxの枠線という指定で、`box-sizing: border-box` のため中身が0pxになり、赤いはずのマーカーがバーの上では白一色で描かれていました（凡例だけが赤い状態）。枠線をやめ、外側に出る `box-shadow` で縁取りするようにして、5px分すべてが赤くなります。
- **タイムラインの配色を整理しました。** 勤務は青一色（従来は青→緑のグラデーションで、1つの区間が2状態に見えていました）、打刻された休憩はティール、現在時刻のマーカーは青系から赤に変更（勤務の青い帯の上で見えなくなっていたため）。
- **トップページを画面幅いっぱいに使うようにしました。** Jobcan側の `1280px` 上限で、広い画面ではカードの左右に余白ができていました。上限を外すのは打刻画面だけで、他のページの幅は変わりません。時計・サマリータイル・タイムラインは可変サイズなので、そのまま広がります。
- **勤務中のパタパタ時計の色を新しいグラデーションに変更しました。**
- **秒の位置に勤務状態を重ねました。** 秒は下揃えになり、その上に状態のピルが乗ります。ピルは塗りつぶしになり、勤務中は点が明滅します。
- **背景アニメーションは1枚のレイヤーだけで描いています。** カード内に `z-index: -1` の装飾レイヤーを1枚敷く方式で、時計・打刻ボタン・タイムラインのDOMには一切触りません（クリックも透過します）。動きはすべてCSSの `transform` / `opacity` で、JSはマウント時にマークアップを組むだけなので毎フレームの処理はありません。ドットの配置は固定シードの疑似乱数で決めているため、SPA遷移で時計が作り直されても背景が組み替わりません。
- **画面キャプチャでは背景アニメーションを外します。** 30秒ループの1コマが写り込むだけで意味がなく、レイヤーが使っている `color-mix()` と `mask-image` は html2canvas が自前で解釈する範囲のため、複製側だけ非表示にしています（表示中のページはそのままです）。

### 設定（ポップアップ）

- **「秒を表示」の設定を廃止しました。** 秒は常に表示されます。
- **時計のサイズに XL / 2XL を追加しました。**
- **「背景アニメーション」の選択を追加しました。** 7択（なし＋6種類）のプルダウンです。変更は開いているタブに即時反映され、設定は `storage.sync` に保存されます。

## Version 1.3.2

レイアウトの整理と、1.3.1で入れた機能の不具合修正が中心です。

### 工数の入力ページ

- **小数点が打てるようになりました。** `2.5` → `2:30` の入力は、Jobcan側のキー制限で「.」自体が入力できず使えませんでした。ピリオド（`.` `．` テンキーの`.`）だけを通すようにして修正しました。
- **実績の表示をゼロ埋めしました。** `0:30` ではなく `00:30`。他の行と桁が揃います。
- **「追加」直後でも候補の並びが正しくなりました。** 行を増やすとプロジェクト候補が一瞬あとにJobcan標準のコード順リストに戻ることがありましたが、ピン留め／最近使った順のまま表示されます。
- **プロジェクトIDをホバーのポップオーバーで表示するようにしました。**

### 工数の一覧ページ

- **その日の行がまとめてハイライトされます。** 1日に複数入力があると、マウスを乗せた1行だけが反応していました。同じ日の行すべてが光ります。
- **ダウンロードボタンを1か所にまとめました。** PDFダウンロード・CSVダウンロード・⚙設定はグラフを挟んで離れていましたが、表示月度の行の右側に集約しました。
- **集計軸のセレクトをグラフの真上に移動しました。**
- 表示月度の行の余白を詰めました。

### 打刻グラフ・時計

- **時間軸をその日の打刻に合わせて自動調整するようにしました。** 固定の06:00–24:00をやめたので、早朝や深夜の打刻でも見やすくなります。軸の外の打刻も端に表示して消えないようにしました。
- **パタパタ時計の表示を修正しました。** 秒の境目で1秒飛ぶことがあった問題と、スクリーンショットで数字が途中で止まる問題を直しました。
- **勤怠データの取得時刻を表示するようにしました。** 古い情報をそのまま見せてしまうことがなくなります。

### サイドメニュー

- **開いたメニューが閉じられない問題を修正しました。** 拡張が折りたたみボタンを隠していたためで、折りたたむと本文の表示幅が広がります。開閉はJobcan標準の動作に戻し、拡張は見た目だけを整えます。
- 折りたたみ時のレイアウトのずれを修正しました。

### その他

- ボタンにマウスを乗せたときに位置がずれる動きをやめました。
- 効果のなかった処理・未使用のスタイルを削除しました（サイドメニューの重複対策、進捗バーなど）。

## Version 1.3.1

工数入力まわりの使い勝手の改善が中心です。

### 工数の入力ページ

- **数字だけで入力できます。** `3` → `3:00`、`2.5` → `2:30`。全角（`１．５`）もそのままでOK。※これまで効いていなかったのを修正しました。
- **プロジェクトIDを隠しました。** 名前の前についている `(2607BfZz0601)` を非表示にして、プロジェクト名だけを表示します。IDはホバーで確認でき、そこから「IDをコピー」「プロジェクト名をコピー」もできます。
- **長い名前もホバーで全文が読めます。** プロジェクト・タスク・メモが列幅で切れていても、マウスを乗せれば全部表示されます。
- **上部にサマリーを追加。** 入力した合計と実労働時間、その差、最終更新を1行で表示。前の日／今日／次の日のボタンで日付を移動できます。
- **ボタンを1列に整理。** 「追加」「スクリーンショット」「保存」を並べました。スクリーンショットはその日の工数レポートを自動で作ります。
- **プロジェクト検索がすぐ使えます。** 一覧をキャッシュするようにしたので、これまで約3秒待ってから効いていた部分一致検索が、最初の1文字目から使えます。

### 工数の一覧ページ

- **入力済みの日数がひと目で分かります。** 「工数入力 12/20 日」と進捗バー、「3 日未入力・1 日不一致」を月の先頭に表示。日付をクリックするとその行までスクロールします。
- ダークモードの表示を改善しました（グラフを含む）。

### 申請一覧（休暇・残業・休日出勤）

- 履歴が0件のときに、空の表ではなくメッセージを表示するようにしました。
- 承認ステータスを色付きのチップで表示するようにしました。

### その他

- ポップアップの見た目を整理し、キーボード操作に対応しました。
- 勤怠データの読み込みを速くしました（打刻一覧 約230ms／勤怠集計 約770ms）。
- 打刻時の紙吹雪が鳴り止まないことがある問題を修正しました。
- ログインに失敗したときにボタンが「ログイン中…」のまま固まる問題を修正しました。
- スクリーンショットのGIF機能を削除しました。
- そのほか、動作を軽くするための内部整理（不要な処理・デッドコードの削除、監視処理の一元管理など）を行いました。

## Version 1.3.0

Jobcanの工数管理ページ（`man-hour-manage`）が全面的に作り直された（モーダル＋`<select>`から、独立ページ＋テキスト入力＋REST API／Web Workerへ移行）ことに対応し、関連機能を新アーキテクチャ向けに再構築しました。

- **工数実績入力ページ（`edit-achievement`）:**
  - モーダルではなく独立ページになった新UIに対応。
  - プロジェクト検索を改善（部分一致対応）: Jobcan標準のオートコンプリートはコード／名称の「前方一致」のみで、下までスクロールしても次の候補しか読み込まれず、名称の途中の語（例「ベネッセ」）で検索できませんでした。標準のオートコンプリート（選択・保存はそのままJobcan側で正しく処理）を活かしつつ、その検索ソースを拡張し、全プロジェクト（約600件、ページング取得・キャッシュ）からの「部分一致」候補も表示するようにしました。任意のキーワードで候補が出るようになります。
    - 補足: 当初は独立したサイドパネル型ピッカーで入力欄ごと置き換えていましたが、その方法だとJobcanの内部状態を経由しないため選択が「未入力」と判定され保存できませんでした。そのため、入力欄の置き換えはやめ、標準ウィジェットの検索のみを拡張する方式に変更しています（ページのjQuery／オートコンプリートに触れるため、`manHourEditSearch.js` は MAIN ワールドで実行）。
  - 工数入力（実績）欄の小数時間入力（例: `3` → `3:00`、`2.5` → `2:30`）に対応。
  - キーボード操作（Shift+Enterで行追加、Cmd/Ctrl+Enterで保存）に対応。
  - 実労働時間との差分を埋める「提案」チップは**廃止**しました。刷新後のJobcanが同等の「差分」列＋「調整」ボタンを標準搭載したため、重複するだけになったためです。
- **工数実績一覧ページ（`achievement-list`）:**
  - Web Workerによる非同期描画（読み込み待ち）に対応。
  - フィルタ（すべて／工数不一致）と、工数実績と総労働時間が一致しない日のハイライトを追加。
  - レポート機能を新APIベースで再構築（プロジェクト別／タスク別集計、KPI、差分・未選択件数）。各日のモーダルを開いて収集する旧方式を廃止し、一覧から直接集計するように変更。
  - レイアウト不具合を修正: 拡張機能の `.col` / `[class*="col-"]` への `width:100%` 指定がBootstrapのフレックス列の挙動を上書きし、「表示月度」ラベルが横幅いっぱいに広がって年月セレクター・表示ボタンが画面外に押し出されていた問題を解消（`.col` / `.col-auto` のフレックスサイズを復元）。
- **内部変更:**
  - 新しいAPIクライアント `manHourApi.js`、ページ別モジュール `manHourEdit.js` / `manHourList.js` を追加。
  - 旧モーダル／`<select>`前提のコード（`tableFilter.js` など）の呼び出しを停止し、フローティングの「工数レポート」ボタンを新ページ・新URLへ更新。

## Version 1.2.5

- 拡張機能のバージョン番号を `1.2.5` に更新しました。
- `changings.md` を更新し、今回のリリース内容を追記しました。

## 1. 基本UI改善

- **メニューの動作と見た目の最適化:**
  - マウスがサイドメニューの範囲外に移動すると自動的にメニューが閉じる仕組みを導入し、操作性を向上。

## 2. 時間管理と作業進捗の視認性向上

- **視覚的な時計表示:**
  - カスタマイズ可能なフリップクロックにより、勤務時間の変化を直感的に確認可能。
  - 作業進捗を示すプログレスバーを導入し、現状の進捗が一目で分かるように。
  - 時間指標にパルスアニメーションを採用し、視覚的な動的フィードバックを提供。

- **工数入力モーダルの改善:**
  - 従来のモーダルウィンドウをサイドパネルに変換し、よりスムーズな操作体験を実現。
  - 勤務時間入力の選択リストを再編成し、カテゴリ別に整理することで情報へのアクセスを容易に。
  - 検索機能も導入、作業内容や時間の素早い検索が可能に。
  - 隣接する日付へのナビゲーション機能や、不一致な勤務時間内容の自動復元機能も追加。

## 3. コンテンツの見やすさ＆操作性の向上

- **情報整理とレイアウト調整:**
  - テーブルヘッダーの簡略化や、折りたたみ可能な情報セクションの導入により、情報整理が一層明瞭に。
  - インタラクティブ要素の再設計により、ユーザーの操作ミスを削減し、直感的なUI体験を提供。

## 4. ユーティリティ機能

- **スクリーンショット機能:**
  - ページ上の情報をキャプチャするためのスクリーンショットボタンを実装。
  - キャプチャ画像はプレビュー表示され、ユーザーが内容を即座に確認可能。
  - フォーム内スクリーンショットの機能も備え、記録の共有や確認が容易に。

- **通知システム:**
  - ユーザーの操作結果や注意事項を即座にフィードバックするための通知システムを強化。

## 総括

Jobcan UI Enhancerは、視認性の向上と操作性の改善を通じて、Jobcan利用時のユーザー体験を劇的に向上させることを目的としています。各機能は日々の業務を支援し、ユーザーがスムーズかつ効率的に作業を進められるよう設計されています。

## 過去の変更（アーカイブ）

### 機能改善とバグ修正

- **UI改善:**
    - 月セレクターと関連ボタンのデザインをより小さく、控えめなスタイルに調整しました。
    - 外部情報パネル (`external-panel-misc`) がデフォルトで折りたたまれるように修正しました。
    - 作業時間オーバーレイに有効期限（例: "YYYY/MM/DDまで有効"）を表示する機能を追加しました。
    - ログインページから不要なスクリーンショットボタンを削除しました。
    - 時計UIのフリップアニメーションとプログレスバーの表示を改善しました。
- **スクリーンショット機能:**
    - メインおよびフォーム内のスクリーンショット取得時の問題を修正しました。
    - `html2canvas` ライブラリやクリップボードAPI (`ClipboardItem`) の利用可否を事前にチェックし、未対応環境でのエラーを防止するようにしました。
    - クリップボードへのコピー処理を改善し、成功/失敗の通知を追加しました。

### 内部構造の変更

- **コードのモジュール化:**
    - 従来の `content.js` を機能ごとに複数のファイル (`ui.js`, `clock.js`, `screenshot.js`, `utils.js`, `main.js` など) に分割し、コードの保守性と可読性を向上させました。

> **削除済みモジュールについて（注記）**
> このアーカイブには、すでに存在しないモジュールの記述が残っていました。整理のため以下に統合します。
> - `manHourModal.js` — 2026年6月のJobcan工数ページ刷新でモーダルUI自体が廃止されたため削除。後継は `manHourEdit.js` / `manHourList.js` / `manHourApi.js` / `manHourEditSearch.js`。
> - `graphEnhancer` / `chartEnhancer.js` — Chart.jsのグラフ内容を書き換える一時的な機能。いずれも削除済み。
> - `tableFilter.js` — 旧工数一覧のフィルタ。刷新でDOMが消滅したため削除。
